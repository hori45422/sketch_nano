

<div class="splash-container">
    <div class="splash">
        <h1 class="splash-head">sketch nano?</h1>
        <p class="splash-subhead">
          What is done cannot be undone.
        </p>
        <p>
            <a href="/draw" class="pure-button pure-button-primary">Get Started</a>
        </p>
    </div>
</div>

<div class="content-wrapper">
  <div class="content">
    <div class="pure-g">
      <% posts.each do |post| %>
        <div class="post u-1-3">
          <h2>
            <%= Rack::Utils.escape_html post["title"] %>
          <button class="like button-xsmall " data-id="<%= post["id"] %>">
            😄
            <%= post["likes"] %>
          </button>
          </h2>
          <div class="post-image">
          <img src="/uploads/<%= post["src"] %>" alt="  <%= Rack::Utils.escape_html post["title"] %>" class="picture">
          </div>
        </div>
      <% end %>

    </div>
      <a class="baragumi" href="mailto:hori@pixiv.com">削除依頼はこちら</a>
  </div>
</div>

<div>

</div>

<script src="/js/dashboard.js"></script>
